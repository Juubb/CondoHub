package com.example.condohub.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================
// Paleta do CondoHub
// O verde remete a sustentabilidade, pilar ambiental do ESG.
// ============================================================

val VerdeCondo = Color(0xFF087F6E)      // cor principal: barras, cards de destaque
val VerdeEscuro = Color(0xFF156B5A)     // botoes e links
val VerdeClaro = Color(0xFFE5F3F0)      // fundos de icone e chips selecionados

val FundoApp = Color(0xFFF5F7F6)        // fundo das telas
val Branco = Color(0xFFFFFFFF)          // superficie dos cards
val TextoPrincipal = Color(0xFF222222)
val TextoSecundario = Color(0xFF808080)
val Divisor = Color(0xFFE3E8E6)

// Cores semanticas (status de reservas e ocorrencias)
val Pendente = Color(0xFF8A6100)
val PendenteFundo = Color(0xFFFDF3DC)
val Confirmado = Color(0xFF156B5A)
val ConfirmadoFundo = Color(0xFFDFF2EC)
val Erro = Color(0xFFB3261E)

// Cores da previsao do tempo, usadas na tela de reserva
val TempoBom = Color(0xFF2E9E5B)        // sem risco de chuva
val TempoInstavel = Color(0xFFE0A800)   // chance media de chuva
val TempoRuim = Color(0xFFC0392B)       // alta chance de chuva
val CeuFundo = Color(0xFFEAF2F8)        // fundo dos cartoes de previsao
